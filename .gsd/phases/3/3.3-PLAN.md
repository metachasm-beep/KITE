---
phase: 3
plan: 3.3
wave: 2
---

# Plan 3.3: Product Detail Specification Cards

## Objective
Establish the individual Object Detail Route (`/collections/[slug]`) mapping CMS Bridge properties specifically defining an artifact's specs (Material, Finish, Dimensions).

## Context
- Refers to `getArtifactBySlug(slug)`.
- Replaces standard Add to Cart with a "Specs" focus and an accented CTA.

## Tasks

<task type="auto">
  <name>Build Object Detail Route</name>
  <files>app/collections/[slug]/page.tsx</files>
  <action>
    1. Create a dynamic route `[slug]/page.tsx`.
    2. Extract `params.slug` properly handling async Next.js 15+ routing properties.
    3. Pass `slug` to `await getArtifactBySlug(...)`.
       If `null`, drop an `import { notFound }` command.
    4. Implement layout structure:
       - **Side 1 (Image/Viewport)**: Create a large placeholder box displaying the object's visual schema with a Cyber Purple accent outline.
       - **Side 2 (Details Pane)**: Series number, Title, Description, and the grid of specifications detailed inside `lib/cms.ts`.
    5. Place a massive Cyber Purple `[ INIT_PURCHASE_SEQUENCE ]` button.
  </action>
  <verify>Navigating to `/collections/void-figure-01` accurately renders context data.</verify>
  <done>Dynamic artifact routes compile and parse metadata seamlessly.</done>
</task>

## Success Criteria
- [ ] Next.js Dynamic Slug Routing is integrated securely.
- [ ] Unfound IDs drop directly to `404` handler.
- [ ] Product presentation features minimal descriptions, high spec focus, and structural precision.
