---
phase: 2
plan: 2.3
wave: 2
---

# Plan 2.3: Admin Interactive Modules

## Objective
Establish the primary interfaces within the Admin Dashboard to manage inventory, track orders, and view delivery updates.

## Context
- .gsd/ROADMAP.md (Phase 2)
- Must construct visually distinct tables for the administrative backend to align with VOIDLAB styling.

## Tasks

<task type="auto">
  <name>Build Inventory Management Interface</name>
  <files>app/admin/inventory/page.tsx, components/admin/InventoryTable.tsx</files>
  <action>
    1. Create a raw data grid for `InventoryTable.tsx` to list placeholder items (e.g., VOID FIGURE 01, NEUROFORM 01).
    2. Columns: Item ID, Title, Stock, Action (Edit/Delete).
    3. Include an "Add Artifact" CTA button using the Cyber Purple accent.
    4. Provide the page implementation at `/admin/inventory`.
  </action>
  <verify>Check `/admin/inventory` navigates and renders correctly.</verify>
  <done>Inventory interface exists and works dynamically rendering placeholder lists.</done>
</task>

<task type="auto">
  <name>Build Orders & Deliveries Tracking</name>
  <files>app/admin/orders/page.tsx, components/admin/OrdersTable.tsx</files>
  <action>
    1. Similar to the inventory approach, construct an `OrdersTable.tsx` for managing customer purchases.
    2. Columns: Order ID, Timestamp, User Email, Deliverable State (e.g., PENDING, SHIPPED, DELIVERED).
    3. Display a toggle for state filtering.
    4. Provide the page implementation at `/admin/orders`.
  </action>
  <verify>Check `/admin/orders` successfully renders the tracking grid.</verify>
  <done>Order tracking and delivery updates screen is structured and styled.</done>
</task>

## Success Criteria
- [ ] `/admin/inventory` exists showcasing tabular item controls.
- [ ] `/admin/orders` allows tracking order status and fulfillment operations.
- [ ] Overall styling follows the strict, tech-forward aesthetic detailed in SPEC.md.
